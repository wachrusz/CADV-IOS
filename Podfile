# Uncomment the next line to define a global platform for your project
#platform :ios, '13.0'
workspace 'CADV'

target 'CADV' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CADV
  pod 'SwiftyJSON'
  pod 'Firebase'
  pod 'RealmSwift'
  target 'CADVTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CADVUITests' do
    # Pods for testing
  end
end
